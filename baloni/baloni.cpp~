#include <cstdio>
#include <cassert>
#include <algorithm>
#include <vector>

using namespace std;

const int N_MAX = 1000000;

int N;
vector<int> p[N_MAX];



int main() {
#ifdef EVAL
    freopen("input.txt", "r", stdin);
    freopen("output.txt", "w", stdout);
#endif
    
    int u;
    
    scanf("%d", &N);
    for (int i = 0; i < N; i++){
        scanf("%d", &u);
        p.push_back(u);
    }
    
    for (int i = 0; i < p.sizeof(); i++)
        printf("%d ", p[i]);
    printf("\n");
 
    
       
    return 0;
    
}
